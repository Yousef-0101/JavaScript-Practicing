// ForEach executes a provided function once for each array element and returns nothing
let friends = ["YOusef", "Tasneem", "Basmala", "Ziad", "Said"];

friends.forEach(function (element) {
	console.log(element);
});
